# PRD｜手写 Live（工作名）

**作者**：—
**日期**：2026-04-25
**状态**：Draft v0.1
**关联文档**：`01-产品探索-手写Live.md`、`Example.html`（笔刷 + 录制 demo）、`Poster template overlay APP demo.html`（排版模板 demo）
**Stakeholders**：产品（你）/ 设计 / 前端 / 增长

> 本 PRD 采用 PM Skills 的 8 段模板（来自 `pm-execution/skills/create-prd` 与 `commands/write-prd`），融合写法。
> **范围**：本 PRD 聚焦 **MVP（Phase 1）**；Phase 2/3 仅用一句话占位，避免过度承诺。

---

## 1. Executive Summary（2–3 句话讲清楚）

我们要做一个移动端 P 图工具「手写 Live」，让普通用户在 **2 分钟内**把一张照片变成一张**真正能直接存相册的 iOS Live Photo**（不是 MP4，不是 GIF）。它把 ① 字体邪修变形（**田字格中线轴 + 八角自由变换**，对应参考图 1+2）、② 排版圈圈模板、③ 一笔一划过程录制 + 一键存 Live Photo 四件事拼成一条线性主流程。

**两个核心差异化**：
- **「过程感」直接是 Live Photo**：用户长按图片自动播放手写过程，**不是** MP4 / GIF，不是「先下载视频再打开」。
- **「邪修字」傻瓜级**：用田字格中线轴一拖即得图 2 的疏密效果，无需 PS / Procreate。

MVP 先在 H5 验证：2 分钟内产出 Live Photo 的转化率 + 一键存相册成功率 + 外部分享率。

---

## 2. Background & Context

### 2.1 我们为什么现在做

- **市场信号**：2025–2026 年小红书/抖音/IG「手写体封面」「邪修字」类话题持续高分发，单条爆款下评论高频问「这是什么 App」「教程在哪」。
- **既有积累**：`Example.html` 已经把「6 种笔刷 + 自动排版 + 一笔一划过程录制成视频」走通；`Poster template overlay APP demo.html` 已经把「红框 + 编号圆圈」模板叠加体验走通。整个端到端链路只缺一环：**字本身的邪修变形（方框形变 + 内部结构调整）**。
- **技术拐点**：OpenType.js + 现代浏览器 Canvas Warp 已经成熟，移动 H5 上做矢量字形自由变换可以稳定 30fps 以上，无需 native。
- **竞品现状**：醒图、美图秀秀有「手写字」但不带邪修变形 + 不带过程录制；Procreate 能做但门槛高、不能直接出 Live；专门 P 图变字工具暂无明显头部。

### 2.2 与三张参考图的对应关系

| 参考图 | 表达的能力 | 在本 PRD 中对应 |
|--------|----------|----------------|
| 图 1：调整方块字外形 → 不规则四边形 | **8 控制点透视变换（外框邪修）** | F2.1（P0） |
| 图 2：调整文字结构 → 有疏有密 | **田字格中线轴 2×2 mesh warp（内部邪修）** | F2.2（**P0**） |
| 图 3：邪修拆解流程（拼合→液化→笔刷描） | 整体线性主流程 | F1（信息架构）+ F4（描画 / 录制） |

> **关键设计洞察**：图 2 不需要「真液化」就能做出。把字切成田字格 4 象限，左右拖中竖轴 → 左部首压缩、右部首扩张，就是参考图 2 「邪」字右半部分变大的效果。这是产品同学提出的核心简化方案，**比真液化轻量 100 倍且 100% 矢量可逆**。

### 2.3 为什么 Live Photo 一定要直接出（不是 MP4）

竞品（醒图、美图等）的「过程视频」体验路径：
1. 在 App 里出视频 → 2. 保存到相册（MP4） → 3. 用户发朋友圈/小红书时，是普通视频 → 4. 没有「长按预览」的魔法感

我们的体验路径：
1. 在 H5 里出 Live Photo → 2. **一键存相册（Live Photo 类型）** → 3. 用户在朋友圈/小红书发的就是 Live 图 → 4. **观众长按 = 看到一笔一划手写过程**

这是「**让传播链路里每一帧用户体验都默认是 Live**」，而不是「让用户多一步操作」。**这是社交传播效率上的代际差**。

---

## 3. Objectives & Success Metrics

### 3.1 Goals（要达成的）

1. **创作完成率**：新用户首次进入 → 成功导出一条 Live 视频/图的占比 ≥ **35%**（对标醒图新手首次出图率 ~50%，但我们流程更长，留足空间）。
2. **首次惊艳**：首次产出的 Live 视频，**用户主动分享率（点击「分享」按钮）≥ 25%**。
3. **传播带新**：带水印分享 → 外部点击进入官网/落地页的 Click-through ≥ **5%**，外部点击 → 注册/试用转化 ≥ **15%**。
4. **D1 / D7 留存**：D1 ≥ 35%，D7 ≥ 12%（H5 工具型基线）。

### 3.2 Non-Goals（明确不做）

1. **不做生成式 AI 造字**（成本、版权、可控性都不划算）。
2. **不做模板/UGC 市场**（Phase 3 之后；MVP 先官方供给）。
3. **不做账号社交关系链**（不做 follow / feed）。
4. **不做桌面端**（PC 用户用 Procreate 即可）。
5. **不做付费墙**（MVP 全部免费，先验证留存与传播）。

### 3.3 Success Metrics（KR 表）

| 指标 | 现状 | 目标（上线 90 天） | 测量方式 |
|------|------|------------------|---------|
| 新用户首次成功导出率 | 0 | ≥ 35% | 漏斗：上图 → 选字 → 描画 → 导出 |
| **iOS 用户「成功存为 Live Photo」率** | 0 | ≥ **70%**（iOS 用户中） | 跳转 Shortcut 后回流到「确认存相册」按钮的比例 |
| 首次产出 → 主动分享率 | 0 | ≥ 25% | 点击「分享」按钮事件 / 成功导出数 |
| **Live Photo 在朋友圈/小红书的「长按预览」触发率（自报告）** | 0 | ≥ 40%（被分享的 Live Photo） | 用户问卷 + 落地页访问者来源识别 |
| 单视频外部带新转化（CTR×注册率） | 0 | ≥ 0.75% | 水印 UTM 链接埋点 |
| D7 留存（再次创作） | 0 | ≥ 12% | 再次完整产出 1 条 Live 的用户 / D0 |
| Mom-test 时长（5 名素人首次产出耗时） | n/a | ≤ 3 min（中位数） | 可用性测试录屏计时 |

---

## 4. Target Users & Segments

> 详见探索文档 §2。本 PRD MVP 锁定 **S1 + S2**。

| 段位 | 人群 | 核心 Job | 占比预期（MVP 期） |
|------|------|---------|------------------|
| **S1 跟风创作者** | 18–28 岁，刷小红书/抖音的图文消费者 | 复刻爆款封面 | 70% |
| **S2 内容博主** | 千–万粉博主 | 形成稳定视觉签名 | 20% |
| 其他（S3/S4 顺带） | 字体爱好者 / 礼物场景 | 造字 / 仪式感 | 10% |

**约束**：
- 主要在 **iOS Safari + Android Chrome H5** 上运行。
- 主要语言：**简体中文**；繁体/英文 Phase 2。

---

## 5. User Stories & Requirements

> 用户故事按 **P0 / P1 / P2** 分级。P0 必须在 MVP 内完成，缺一不可。

### 5.1 P0 — Must Have（MVP）

| # | User Story | Acceptance Criteria |
|---|-----------|--------------------|
| **US-1** 上图与底图 | 作为用户，我可以上传一张本地图片当底图，并能锁定/解锁、调整位置 | 1) 支持 jpg/png/heic；2) 上传后默认居中铺满；3) 长按可锁定，避免描画时误移动；4) 复用 `Example.html` 已有上传逻辑 |
| **US-2** 选标准字 | 作为用户，我能从 1 套官方字库（默认霞鹜文楷）输入 1–6 个汉字，作为「参考字」铺在底图上 | 1) 输入框直接输入；2) 字以矢量 SVG 渲染；3) 默认半透明粉色（与图 1 对齐），可关闭 |
| **US-3a** 外框邪修：8 控制点（图 1） | 作为用户，我可以用 4 角 + 4 中点共 8 个控制点把每个字的外框任意拖成不规则四边形 | 1) 实时矢量透视变换，无锯齿；2) 可双击重置；3) 中端机（iPhone 12）≥ 30fps |
| **US-3b** 内部邪修：田字格中线轴（图 2，**核心创新**） | 作为用户，每个字默认显示一个田字格（**1 横轴 + 1 竖轴**），拖竖轴 → 左右部首挤压/扩张，拖横轴 → 上下部首挤压/扩张 | 1) 默认中线在 50%/50%；2) 拖动竖轴范围 20%–80%（避免极端形变）；3) 实时显示比例提示「左 35% / 右 65%」；4) **2×2 mesh warp 实现，60fps 无压力**；5) 可双击重置；6) 与 US-3a 可叠加（先内部、再外框） |
| **US-4** 排版套圈 | 作为用户，我能选 1 个排版模板（4–6 选 1），把字按编号圆圈位置自动摆放 | 1) 复用 `Poster template overlay APP demo.html` 的 sq/v/h 模板；2) 点击模板后字自动落到对应圆圈；3) 整组可拖动缩放 |
| **US-5** 描画笔刷 | 作为用户，我能选笔刷颜色 + 大小，沿参考字一笔一划描画 | 1) 复用 Demo 1 全部 6 种笔刷；2) 描画过程记录每条 path 时序 |
| **US-6** 一键隐藏参考 | 作为用户，描完后我能一键隐藏「参考字 + 田字格 + 圆圈 + 红框」，只看到我的笔迹 | 1) 单按钮切换；2) 切换 ≤ 100ms；3) 切换不影响录制时序 |
| **US-7a** 录制过程视频（Android & 兜底） | 作为用户，我能导出「一笔一划写完字」的视频（≤ 10 秒） | 1) MP4 / WebM；2) 9:16 与 1:1 两种比例；3) 视频中**只有我的笔迹动画**（参考字与田字格不出现）；4) 默认带角标水印 |
| **US-7b** **iOS 一键存 Live Photo（核心创新）** | 作为 iOS 用户，我点「存到相册」按钮，结果是一张**真正的 Live Photo**，长按可播放手写过程 | 1) 第一次使用：引导一键安装 Apple Shortcut「Save Live Photo」（提供深链）；2) 之后每次：网页生成 .HEIC（封面）+ .MOV（带 `com.apple.quicktime.content.identifier` UUID 与 `still-image-time` 时间戳轨道）→ 跳转 Shortcut → 自动写入相册；3) iOS 16+ Safari 全量支持；4) **降级链路**：用户拒装 Shortcut 时，自动改用 AirDrop 自己 / 下载 zip + 教程 |
| **US-8** 导出静态图 | 作为用户，我能直接导出 PNG（不带过程） | 复用 Demo 1 的 `btn-save-img` 逻辑 |
| **US-9** 撤销 | 作为用户，每一步都可以撤销 | 至少 20 步，跨主流程也能回退（取消邪修、重选模板等） |

### 5.2 P1 — Should Have

| # | User Story | Acceptance Criteria |
|---|-----------|--------------------|
| US-10 田字格升级为 3×3 井字格 | 用户能切换到 3×3 网格做更精细的疏密调整 | 2 横轴 + 2 竖轴 = 4 拖动元素；适合上下结构 + 左中右结构字 |
| US-11 复刻爆款模板包 | 用户在首页能直接选「本周爆款」模板，一键加载（图 + 字 + 排版 + 笔刷预设） | 每周更新 ≥ 3 个；模板预览图 |
| US-12 首次沉浸引导 | 新用户首次进入走 30s 引导：跟着做出一个「邪」字效果 | 5 步分镜；可跳过；引导内首次让用户拖动田字格中线轴感受魔法 |
| US-13 草稿 | 操作中断后回到 App 能继续编辑 | LocalStorage 自动存最近 3 个草稿 |
| US-14 分享带水印 + UTM | 视频右下角默认水印「@手写Live」+ 二维码；点击跳落地页 | 可付费去水印（Phase 2 上线付费时打开） |
| US-15 Android Motion Photo 适配 | Android 用户能导出小米/OPPO/Google 的 Motion Photo 格式 | 用 XMP 嵌入视频，相册自动识别为「实况照片」 |

### 5.3 P2 — Nice to Have / Future（Phase 2+）

| # | User Story | 备注 |
|---|-----------|------|
| US-20 真液化笔刷 | forward warping，可在田字格之外做不规则形变 | 性能挑战；田字格已能覆盖 80% 场景 |
| US-21 笔刷录制时序剪辑 | 用户可调单笔时长、加停顿、加变速 | 进阶用户向 |
| US-22 iOS 原生 App 壳 | WKWebView + PHAssetCreationRequest 直接写入相册（免 Shortcut） | 把 P0 US-7b 的 Shortcut 路径升级为「真一键」 |
| US-23 字库扩展 | 接入 3–5 套商用字体 | 含许可清单 |
| US-24 AI 建议 | 基于底图主色调建议笔刷颜色 | 轻 AI |
| US-25 模板市场 / UGC | 用户上传自己的字形+排版组合供他人使用 | 需要后端账号 |
| US-26 共编 / 邀请好友 | 分享链接共同编辑 | 增长机制 |
| US-27 田字格智能吸附部首 | 接入 cjk-decomposition IDS，让中线轴自动吸附到部首交界处 | 让普通用户更准 |

---

## 6. Solution Overview

### 6.1 信息架构 / 主流程（线性 5 步）

```
首页（模板包 + 空白创作）
    │
    ▼
① 上传图片 ─▶ ② 选字 + 方框邪修 ─▶ ③ 套排版模板 ─▶ ④ 选笔刷描画 ─▶ ⑤ 隐藏参考 + 导出
                  ▲                                           │
                  └─────── 任意步骤可撤销 / 跳回 ──────────────┘
```

### 6.2 关键交互（Wireframe 文字描述）

#### 第二步：选字 + 方框邪修

```
┌────────────────────────────────┐
│  [底图，淡淡呈现]               │
│                                │
│   ┌──┐  ┌──┐  ┌──┐             │
│   │邪│  │修│  │版│  ← 标准字   │
│   └──┘  └──┘  └──┘   半透明粉   │
│                                │
│   ┌─◇──◇──┐                    │
│   ◇       ◇  ← 选中后 8 个控   │
│   │  邪   │     制点出现       │
│   ◇       ◇                    │
│   └─◇──◇──┘                    │
│                                │
│   [输入字]  [拆部首]  [重置]    │
└────────────────────────────────┘
        Bottom toolbar
[选字] [邪修] [排版] [笔刷] [导出]
```

#### 第五步：导出

```
┌────────────────────────────────┐
│ 选择导出格式：                  │
│  [□ 高清 PNG]                   │
│  [✓ Live 视频 9:16 (默认)]      │
│  [□ Live 视频 1:1]              │
│                                │
│ ☑ 带水印（去水印 → 升级 PRO）   │
│                                │
│        [生成 ✨ ]               │
└────────────────────────────────┘
```

### 6.3 Key Features 详述

#### F1 · 主流程线性导航（P0）
- 底部固定 5 步骤标签栏，**当前步高亮 + 完成步打勾**。
- 任何一步可点回去修改，但 ④ → ⑤ 之间会提示「确定已描完？」。
- 复用 Demo 1 的 `bottom-nav` 框架。

#### F2 · 字形邪修工坊（核心创新模块）

##### F2.1 外框邪修：8 控制点透视变换（P0，对应图 1）

- **数据模型**：每个字一个 `transform` 对象，字段为 `{ corners: [tl, tr, br, bl], midpoints: [t, r, b, l] }`，每个点是 `{ x: 0..1, y: 0..1 }` 的归一化坐标，初始为标准矩形。
- **技术实现**：
  1. `opentype.js` 解析字体文件 → 把字渲染为 SVG path（矢量）。
  2. 用 4 个角点求一个 **3×3 透视变换矩阵**（projective / homography），用 SVG `<g transform="matrix3d(...)">` 包裹 path（CSS / SVG 都原生支持）。
  3. 4 个边中点用来对每条边做 **二次曲线拉拽**（从直边变弧边，做"瘦弯"效果）→ 实现时是把 path 在每条边方向上做线性 warp，1ms 级。
- **性能预算**：iPhone 12 ≥ 30fps；触摸延迟 ≤ 50ms。
- **降级方案**：低端机（用 `navigator.hardwareConcurrency` < 4 + screen DPR < 2 检测）→ 拖动时显示低分辨率预览，松手再 commit 高清渲染。

##### F2.2 内部邪修：田字格中线轴 2×2 mesh warp（P0，对应图 2，**核心创新**）

> 这是本产品最关键的差异化交互，由产品同学提出的简化方案。完整 spec 如下。

**用户认知模型**：
> 「字 = 一张图 = 田字格。中间一横一竖两条线把字分成 4 块。拖竖线 → 左右两半的胖瘦比例变；拖横线 → 上下两半的胖瘦比例变。」

**视觉示意**（实际界面）：

```
              拖动我 ↓
   ┌────────┰────────┐
   │  左上  ┃  右上  │
   │   ▢   ┃   ▢   │
拖→┠━━━━━━━━╋━━━━━━━━┨← 拖动我
   │  左下  ┃  右下  │
   │   ▢   ┃   ▢   │
   └────────┸────────┘

       中竖轴 vx ───┘
       中横轴 hy ─→ 都默认 50%
```

**数据模型**：每个字一个 `mesh` 对象，仅 2 个字段：
```ts
type Mesh2x2 = {
  vx: number;  // 0.2..0.8，默认 0.5（中竖轴位置）
  hy: number;  // 0.2..0.8，默认 0.5（中横轴位置）
};
```

**渲染算法**（本质是把 1 张图按 4 象限分别缩放重绘）：

| 象限 | 源矩形（标准字） | 目标矩形（变形后） |
|------|----------------|------------------|
| 左上 | (0, 0, 0.5, 0.5) | (0, 0, **vx**, **hy**) |
| 右上 | (0.5, 0, 0.5, 0.5) | (vx, 0, **1-vx**, **hy**) |
| 左下 | (0, 0.5, 0.5, 0.5) | (0, hy, **vx**, **1-hy**) |
| 右下 | (0.5, 0.5, 0.5, 0.5) | (vx, hy, **1-vx**, **1-hy**) |

**技术实现**：
- **方案 A（推荐，纯 SVG）**：把 SVG path 复制 4 份，每份用 `<clipPath>` 截取对应象限 + `<g transform="matrix(...)">` 缩放到目标位置。**浏览器原生 GPU 合成层，60fps 在 iPhone 8 都没压力。**
- **方案 B（位图）**：把字栅格化为 ImageBitmap，4 次 `ctx.drawImage(bitmap, sx, sy, sw, sh, dx, dy, dw, dh)`，每帧 4 次 GPU blit，30fps 起。

**为什么这个方案碾压"真液化"**：
1. **性能**：只更新 2 个浮点数 + 4 个矩阵，O(1)；液化是 O(N²) 像素操作。
2. **可逆**：保存 `{vx, hy}` 即可重现，2 个数字。
3. **矢量**：导出仍是矢量，不损失分辨率。
4. **认知**：用户脑中"田字格"模型 = 小学生都懂；液化要解释半天。
5. **足以覆盖图 2 效果**：左右结构（如「邪」「林」「江」）拖竖轴；上下结构（如「字」「思」「春」）拖横轴；包围结构（如「国」「回」）双向都拖。

**交互细节**：
- 选中字时，4 角浮起 8 控制点（F2.1） + 中间出现 1 横 1 竖两条蓝色虚线轴（F2.2）。
- 拖竖轴时，实时显示「左 35% / 右 65%」浮动 hint，松手 1 秒后消失。
- 双击轴 → 重置为 50/50。
- 长按轴 → Phase 2 进入「智能吸附部首交界」模式。
- F2.1 + F2.2 **可叠加**：先 mesh warp（影响内部）再 perspective（影响外框），矩阵复合，性能不变。

##### F2.3 真液化（P2，砍出 MVP）

forward warping（液化笔刷）只在 Phase 2 之后做。MVP 阶段 F2.1 + F2.2 已能覆盖参考图 1 + 图 2 的 90% 效果。

#### F3 · 排版模板（P0）
- 直接复用 `Poster template overlay APP demo.html` 的 JSON 模板格式。
- MVP 选 4–6 个高频模板：方形 1 个、竖排 2 个、横排 2 个、自定义 1 个。
- **关键改造**：把模板里的「圆圈」从「装饰」升级为「字位 anchor」——选字后，每个字自动吸附到一个圆圈中心，圆圈大小决定字的初始尺寸。

#### F4 · 笔刷描画（P0）
- 100% 复用 Demo 1 的 6 种笔刷：极致顺滑 / 中空标题 / 胖胖圆体 / 可爱手抖 / 素描铅笔 / 颗粒粉笔。
- **关键改造**：drawHistory 增加 `track` 字段，区分 `guide`（参考字、圈圈，不录入视频）与 `ink`（笔刷，录入视频）。

#### F5 · 一笔一划录制 → 真 Live Photo 输出（P0，**核心创新模块**）

> 这是产品的另一个关键差异化点。竞品给 MP4，我们给真 Live Photo。

##### F5.1 录制底层（沿用 Demo 1，做轨道改造）

- 复用 Demo 1 的 `MediaRecorder` 实现，但 `drawHistory` 增加 `track` 字段：
  - `track === 'guide'`：参考字、田字格、排版圈圈 → **不录入** Live Photo
  - `track === 'ink'`：用户笔刷描画 → **录入** Live Photo
- 总时长固定 3 秒（Apple Live Photo 标准时长），按笔画数自动调速：
  - 笔画数 ≤ 8：每笔 350ms
  - 笔画数 9–20：每笔 150ms
  - 笔画数 > 20：每笔 80ms（极致压缩，仍保留过程感）
- 输出 9:16 / 1:1 / 4:3 三种比例，1080p。

##### F5.2 真 Live Photo 配对生成（核心）

**原理**（已经过 livephoto.online、`flashlab/motion-live-photo` 验证）：

> Live Photo = `.HEIC` 静态封面 + `.MOV` 视频，两者通过同一个 **UUID（assetIdentifier）** 配对。

**生成步骤**（**全部在浏览器本地完成，0 上传**）：

1. **生成 UUID**：`crypto.randomUUID()` 生成一个全局唯一标识符。
2. **HEIC 封面**：
   - 从 fabric canvas 取最后一帧 PNG → 用 **libheif-js / heic-encode**（WASM）转 HEIC。
   - 用 `@aidin36/xmp` 写入 `MakerNotes` 的 `[17 : assetIdentifier=UUID]`。
3. **MOV 视频**：
   - `MediaRecorder` 输出 webm/mp4 → 用 **FFmpeg.wasm** 转码为 H.264 + HEVC 兼容的 .MOV。
   - 用 mp4box.js 写入 metadata：
     - `com.apple.quicktime.content.identifier` = UUID
     - 加一条 timed metadata track：`com.apple.quicktime.still-image-time` = `0xFF`（指向静帧时间点）
4. **打包交付**：把 `xxx.heic` + `xxx-motion.mov` 两个文件一起交给用户。

##### F5.3 「一键存相册」交付路径（关键体验设计）

**为什么这是关键**：iOS Safari 不能直接写 Photos 库，所以必须设计一条「不让用户感到麻烦」的交付路径。

**首次使用流程**：

```
用户点「存到相册」
    │
    ├─→ 检测 iOS：是
    │       │
    │       ├─→ 检测「Save Live Photo」Shortcut 是否安装：否
    │       │       │
    │       │       ├─→ 弹窗：「首次使用需要 5 秒安装一次小工具，之后每次都是一键」
    │       │       └─→ 跳转 https://www.icloud.com/shortcuts/xxx 一键安装
    │       │
    │       └─→ 已安装：
    │               ├─→ 浏览器把 .heic + .mov 写到 iOS 临时下载目录
    │               └─→ deep link 跳 shortcuts://run-shortcut?name=Save%20Live%20Photo&input=...
    │                       └─→ Shortcut 执行：把两个文件配对存进相册
    │                               └─→ 回流 Safari，弹「✅ 已存为 Live Photo」
    │
    └─→ 检测 Android：直接下载 9:16 MP4（兜底）
                    + Phase 1.5 适配 Motion Photo（小米/OPPO/Google）
```

**降级链路**（覆盖各种边界情况）：

| 场景 | 兜底方案 |
|------|---------|
| 用户拒装 Shortcut | 提供 **AirDrop 自己** 流程：跳系统分享 → 选自己设备 → iOS 自动把配对识别为 Live Photo |
| Android 用户 | 默认 9:16 MP4 + 1 张封面 PNG（不做 Live Photo 概念） |
| Android 小米/OPPO/Google 设备 | P1：用 XMP 嵌入「Motion Photo」格式，相册自动播放 |
| 微信内置浏览器（最差兼容） | 提示「请用系统浏览器打开」+ 复制链接按钮 |
| Shortcut 回流失败 | 弹「请到相册查看，找不到？点这里看图文教程」 |

##### F5.4 性能预算

| 环节 | 目标 | 极限设备 |
|------|------|---------|
| HEIC 编码 | ≤ 800ms | iPhone 12 |
| MOV 编码（3 秒视频） | ≤ 2.5s | iPhone 12 |
| metadata 注入 | ≤ 200ms | iPhone 12 |
| **总「点击导出 → 跳 Shortcut」时长** | **≤ 4s** | iPhone 12 |
| 同上 | ≤ 8s | iPhone XR |

> 超过 4 秒会显示进度条 + 文案「正在合成 Live 魔法...」（带粒子动画转移注意力，借鉴美图秀秀）。

##### F5.5 Phase 2：原生 App 壳

如果 MVP 上线后 Live Photo 这条链路验证有效（≥ 70% iOS 用户成功保存），立即立项一个**最小 iOS 容器 App**：
- WKWebView 包 H5
- Native Bridge 一个 `saveLivePhoto(heic, mov)` 接口
- 调 `PHAssetCreationRequest` 直接写相册
- **0 Shortcut 步骤、0 跳转、0 学习成本**
- App Store 审核风险低（不算照片类，算工具类）

#### F6 · 默认水印 + 分享（P0）
- 视频导出时在右下角合成水印「@手写 Live · live.xxx.com」+ 小二维码。
- 点击「分享」直接调起系统分享 sheet（Web Share API）。
- 落地页 UTM 跟踪。

#### F7 · 撤销 / 草稿 / 重置（P0 撤销，P1 草稿）
- 撤销栈 ≥ 20 步，含「跨步骤」回退（如从描画回到邪修，标准字保留邪修态）。
- 草稿用 LocalStorage，仅存 JSON + 缩略图，不含原图二进制（Phase 2 上传后再做云草稿）。

### 6.4 Technology

| 模块 | 技术选型 | 理由 |
|------|---------|------|
| 框架 | 纯 H5 + Vite + TypeScript | 上线快、可分发到任何浏览器 / 微信内置 |
| 画布引擎 | fabric.js 5（沿用 Demo 1） | 已验证 |
| 字体解析 | opentype.js | 业界标准，移动端可用 |
| 字形外框透视（F2.1） | SVG `matrix3d` + 自实现 homography 求解 | 矢量、可逆、原生 GPU |
| **字形内部 mesh（F2.2）** | **SVG `<clipPath>` + 4×`<g transform>` 复合** | **60fps、矢量、O(1) 状态** |
| 录制 | MediaRecorder（Demo 1 已有）+ FFmpeg.wasm 转 .MOV | iOS 14.3+ 原生支持 H264 录制；FFmpeg.wasm 用于产 Live Photo 标准 .MOV |
| HEIC 编码 | libheif-js（WASM） | 浏览器端唯一可用方案 |
| **HEIC/MOV metadata 注入** | **mp4box.js + `@aidin36/xmp`** | 写入 `assetIdentifier` UUID + `still-image-time` |
| **Live Photo 交付** | **Apple Shortcuts deep link（首次安装一次）** | 唯一在 H5 上接近"一键"的路径 |
| 分享 | Web Share API + 自定义 fallback | 移动端覆盖 |
| 部署 | 静态站点（Vercel / Cloudflare Pages） | 0 后端 MVP |
| 埋点 | 自托管 Plausible / 友盟 H5 | 隐私友好 |

> **资产体积预算**：FFmpeg.wasm（核心包）~ 11MB（gzip ~ 3MB），libheif-js ~ 2MB，opentype.js ~ 350KB。**全部懒加载**（只在用户点「导出 Live」时才下载）。首屏 JS ≤ 200KB。

### 6.5 Assumptions（待验证）

> 与探索文档 §5 同步；这里只列影响 MVP 的强假设。**新加 4 条 Live Photo 与 mesh 相关的关键假设。**

| # | 假设 | 验证手段 | Owner |
|---|------|---------|-------|
| A1 | iPhone 12（A14）能跑 30fps 实时透视变换（F2.1） | 1 天技术 spike | 前端 |
| A2 | MediaRecorder 在 iOS Safari 16+ 能稳定输出 MP4（备选 WebM） | 跨设备测试 | 前端 |
| A3 | 默认水印不会让 ≥ 80% 用户主动遮盖/裁掉 | 灰度上线 1 周 + 用户访谈 | 增长 |
| A4 | 用户能不看教程通过 8 控制点拖出图 1 效果 | 5 人 Mom-test | 设计 |
| **A5** | **用户能不看教程通过田字格中线轴拖出图 2 效果**（F2.2） | **5 人 Mom-test，看是否能 30 秒内做出"邪"字右半放大** | **设计** |
| **A6** | **用户愿意为「真 Live Photo」首次安装一次 Apple Shortcut** | **A/B 测：Shortcut 引导 vs 直接给 MP4，看分享率差异是否 ≥ 50%** | **增长** |
| **A7** | **HEIC + MOV 配对在 iOS 16/17/18 三个版本 Safari 都能跑通**（含 Shortcut 回流） | **真机灰度，三版本各 3 台** | **前端** |
| **A8** | **田字格 mesh warp 在 iPhone XR（中低端）能 60fps，不影响实时拖动手感** | **2 小时 spike** | **前端** |
| A9 | FFmpeg.wasm 11MB 懒加载在 4G 网络下可接受（≤ 5s） | 网络模拟测试 | 前端 |

---

## 7. Open Questions

| # | Question | Owner | Deadline |
|---|----------|-------|---------|
| Q1 | MVP 平台是 H5 还是 iOS App？建议 H5 起步，但 iOS App 壳放进 Phase 2 立项备份 | 产品 | T+3 天 |
| Q2 | 字库 MVP 选哪 1 套？建议默认霞鹜文楷（开源、文艺，匹配跟风用户） | 设计 + 法务 | T+5 天 |
| Q3 | 是否做账号体系？建议 MVP 不做 | 产品 | T+3 天 |
| Q4 | 水印形式：硬水印 vs 角标 + 二维码？取舍 | 增长 | T+10 天 |
| Q5 | 商业化时间表：MVP 全免费 → Phase 2 起付费墙具体放哪个 feature？ | 产品 | Phase 1 上线后 |
| Q6 | 「跟风模板包」内容供给从哪里来？ | 增长 + 设计 | T+10 天 |
| Q7 | 隐私：图片是否需要上传？建议**全本地处理**，0 上传（也是 Live Photo 路径的副产品） | 工程 | T+3 天 |
| **Q8** | **Apple Shortcut 是我们自家做、还是借用现有公开版（livephoto.online 已有）？** 自家做体验更可控、有 brand；借用上线快 | **产品 + 工程** | **T+5 天** |
| **Q9** | **田字格中线轴默认显示还是手动开启？** 默认显示 → 教育成本 0 但首屏视觉重；手动开启 → 干净但用户可能找不到这个核心功能 | **设计** | **T+5 天** |
| **Q10** | **F2.1 八控制点 vs F2.2 田字格，谁先教？** 建议首次引导只教田字格（更直观），8 控制点放进阶教学 | **设计** | **T+5 天** |
| **Q11** | **Live Photo 失败时降级到 AirDrop 还是直接给 MP4？** 关系到留存指标如何计算 | **产品** | **T+10 天** |

---

## 8. Timeline & Phasing

> 用相对时间，不用绝对日期。假设 1 个产品 + 1 设计 + 2 前端的小团队。

### Phase 0｜技术与可用性 spike（**第 1–2 周**，因为新增 Live Photo 链路）

- **W1 工程 spike（决定生死的 3 个）**：
  - **S1**：F2.2 田字格 2×2 mesh warp 在 iPhone XR / 12 / 15 三档机的 fps（A8 假设验证）—— 0.5 天
  - **S2**：HEIC + MOV 配对生成 + Apple Shortcut 一键跳转，端到端走通 1 次（A7 假设验证）—— 1.5 天
  - **S3**：FFmpeg.wasm + libheif 包体积 + 懒加载策略（A9 假设验证）—— 0.5 天
- **W1 设计**：田字格中线轴 + 8 控制点交互的高保真原型。
- **W2 用户测试**：5 人 Mom-test（A5、A6、A4 验证）。
- **输出**：技术决策备忘录 + 测试报告 + Live Photo 链路 Demo 视频。
- **Go / No-go 关卡**：S1 < 30fps 或 S2 全失败 → 重新评估方案（如改为 native MVP）。

### Phase 1｜MVP 上线（**第 3–8 周**，约 6 周）

- W3：信息架构 + 主流程串联（基于 Demo 1 + Demo 2 合并代码）。
- W4：F2.1 八控制点 + F2.2 田字格 2×2 mesh warp（**两者都是 P0**）。
- W5：F3 排版模板字位吸附改造 + F4 笔刷链路接通。
- W6：F5 录制改造（双轨）+ **F5.2 Live Photo 配对生成 + F5.3 Shortcut 跳转**（核心）。
- W7：F6 水印分享 + 可用性测试 + 性能优化。
- W8：Beta + 灰度上线 + 公开发布。

### Phase 2｜增长与体验深化（**第 9–15 周**）

- 3×3 井字格升级（US-10）、首次沉浸引导（US-12）、爆款模板包（US-11）、付费去水印（US-14）、Android Motion Photo 适配（US-15）。
- **iOS 原生 App 壳（US-22）立项**：把 Live Photo 路径从 Shortcut 升级为 native 一键。
- 字库扩展、轻 AI 建议。

### Phase 3｜社区化（**第 16 周以后**）

- 真液化笔刷（US-20）、UGC 模板市场（US-25）、共编（US-26）、账号体系、智能部首吸附（US-27）。

---

## 附录 A · 与现有 Demo 的代码复用清单

| 现有文件 | 复用模块 | 改造点 |
|---------|---------|--------|
| `Example.html` | fabric 画布初始化、6 种笔刷、自动排版、`drawHistory`、`MediaRecorder` 录制、4 种纸张底纹、撤回 | drawHistory 加 `track` 字段；MediaRecorder 输出接入 FFmpeg.wasm 转 .MOV；底纹 MVP 暂时关掉 |
| `Poster template overlay APP demo.html` | `TEMPLATES` JSON 数据结构、模板拖拽缩放、整组移动 | 圆圈从「装饰编号」改为「字位 anchor」；红框默认隐藏 |

## 附录 B · 田字格中线轴交互的 ASCII 状态机

```
[未选中字]
   │ (点击字)
   ▼
[选中：8 角点 + 田字格双轴 同时显示]
   │
   ├──(拖角点)────► [外框透视变形中] ──(松手)──► [选中态，外框已变]
   │
   ├──(拖竖轴)────► [左右疏密变形中] ──(松手)──► [选中态，内部已变]
   │       │
   │       └──(实时显示「左 35% / 右 65%」hint)
   │
   ├──(拖横轴)────► [上下疏密变形中] ──(松手)──► [选中态，内部已变]
   │
   ├──(双击竖轴)─► [vx 重置为 0.5]
   ├──(双击横轴)─► [hy 重置为 0.5]
   └──(点空白处)─► [取消选中]
```

## 附录 C · Live Photo 配对文件的 metadata 规范（实现参考）

```yaml
# 静态封面
filename: handwrite_<UUID>.heic
metadata:
  EXIF/MakerNotes/Apple:
    "17": <UUID>           # assetIdentifier，关键

# 动态视频
filename: handwrite_<UUID>-motion.mov   # -motion 后缀防 Safari 同名覆盖
container: QuickTime / MOV
codec: H.264 baseline / 1080p / 30fps / 3s
metadata:
  com.apple.quicktime.content.identifier: <UUID>   # 与 HEIC 同
  tracks:
    - timed metadata track:
        com.apple.quicktime.still-image-time: 0xFF  # 静帧时间点
```

## 附录 D · 后续可衔接的 PM Skills 命令

- `/pre-mortem PRD-手写Live.md` — 跑一次失败前瞻，找隐藏风险（**强烈建议下一步执行，重点针对 Live Photo 链路与田字格交互可用性**）。
- `/write-stories user PRD-手写Live.md` — 把 P0 用户故事拆成可分配给前端的 backlog 条目。
- `/test-scenarios` — 基于 P0 用户故事生成测试用例（**Live Photo 兼容性测试矩阵会很重**：iOS 16/17/18 × Safari/Chrome × Shortcut 装/未装 × 网络好/差）。
- `/north-star` — 等开始内测有数据时，正式定义北极星指标。
- `/plan-launch` — 在 Beta 末期做 GTM 计划（小红书 / IG 种子博主分发）。

