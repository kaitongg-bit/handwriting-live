# 产品前期探索｜手写 Live（工作名）

> 作者：— ｜ 日期：2026-04-25 ｜ 状态：Discovery v0.1
> 方法论：Teresa Torres《Continuous Discovery Habits》产品三人组多视角 ideation（PM / Designer / Engineer），叠加 Opportunity Solution Tree 思路。
> 配套文档：`PRD-手写Live.md`

---

## 0. 一句话定位

**把「在图片上写一行邪修手写字 + 一笔一划过程导出成真 Live Photo」做成傻瓜级 P 图工具，让普通用户产出「看起来像设计师做的」社交内容，并且发出去就是会动的 Live 图，长按就能看到手写过程。**

工作名候选（先用「手写 Live」推进，后续可换）：
- 手写 Live（直白，对应英文 Handwriting Live）
- 邪字 Live（强调「邪修造字」基因）
- 字邪 / 手写邪修（更社区化）
- 描一笔（更动作化）

> **v0.2 关键升级**（基于产品同学反馈）：
> 1. **「直接出真 Live Photo」从 Phase 2 升级到 P0** — 因为「能不能在朋友圈/小红书让观众长按看过程」是社交传播链路的代际差，不是体验细节。
> 2. **字形变形从「8 控制点 + 真液化」改为「8 控制点 + 田字格中线轴」** — 田字格 2×2 mesh warp 是产品同学提出的核心简化方案，比真液化轻 100 倍但能覆盖 90% 邪修效果，是本产品最聪明的创新点。

---

## 1. 机会陈述（Opportunity Statement）

### 1.1 现象与机会
- **趋势：** P 图类内容（小红书、抖音、Instagram）在 2025—2026 年仍是高分发权重，其中**「在照片上加一行手写字」+「过程视频/Live 图」**是当下最容易出爆款的组合之一（参考：小红书「手写体封面」「邪修字体」「PS 液化字」类话题）。
- **痛点：** 想做这种内容的普通用户，目前要打开 **Procreate / 醒图 / PS / Picsart** 多个工具拼接，门槛高、Live 图导出更难。
- **已有积累（自带优势）：**
  - `Example.html` 已经验证了「6 种笔刷 + 自动排版 + 一笔一划录像导出」整条 live 视频生产链路。
  - `Poster template overlay APP demo.html` 已经验证了「红框 + 编号圆圈」的排版模板叠加体验。
  - **缺口仅剩两环**：
    1. **造字邪修：** 让用户从字库选标准字 → 把方块字「邪修化」（外框变形 + 内部疏密结构调整）→ 喂给笔刷链路。
    2. **导出体验：** 让最终产物**不是 MP4，而是真 iOS Live Photo**——用户存到相册即用、发朋友圈/小红书观众长按即看过程。这是从「过程视频工具」跃迁到「Live 图原生工具」的代际差。

### 1.1.1 为什么 Live Photo 直接出是核心战略点（不是工程细节）

竞品（醒图、美图、Snow、Picsart）目前给的全是 MP4。用户在朋友圈发的是普通视频，观众必须**点击播放**才能看到过程，传播链路有一道坎：

```
旧链路：用户做完 → 存 MP4→ 发朋友圈/小红书 → 观众点开 → 看完 → 划走
新链路：用户做完 → 存 Live Photo→ 发朋友圈/小红书 → 观众长按 → 看完 → 「这怎么做的？」
```

**为什么 Live Photo 在传播上是降维打击：**
1. **第一帧就是封面**：feed 流默认展示成图，颜值即吸引力。
2. **长按触发「魔法时刻」**：观众无意识长按 → 突然字开始一笔一划写 → 「卧槽这是 Live」 → 主动评论求教程。
3. **平台原生展示**：小红书、IG、朋友圈对 Live Photo 都做了原生支持（不会被压缩成低质 GIF）。
4. **不占视频位**：朋友圈一组 9 张图都可以是 Live Photo，比发视频多 8 倍传播位。

**技术调研结论（已验证）**：
- iOS Safari 不能直接写 Photos 库 ❌
- 但 **本地浏览器生成 .HEIC + .MOV 配对 → Apple Shortcut 一键存** 完全可行 ✅（已有 livephoto.online、`flashlab/motion-live-photo` 跑通）
- 关键技术栈：`libheif-js` + `FFmpeg.wasm` + `mp4box.js`，**全部 WASM、0 后端、0 上传**
- 用户体验：**首次安装 Shortcut（5 秒）+ 之后每次一键**

**一句话**：从工程上「能不能做到」的答案是**能**，所以这一定要进 MVP P0。

### 1.2 期望产出（Desired Outcomes）
| 维度 | 北极星指标候选 | 输入指标 |
|------|---------------|---------|
| **创作完成率** | 7 日内首次成功导出 Live 视频/图的用户占比 | 上传图片 → 选字 → 描画完成漏斗各环节转化率 |
| **社交传播** | 单用户人均分享次数（Share / DAU） | 带水印分享比例、外站二次传播率 |
| **留存** | W1/W4 创作留存（再次产出 Live 内容的用户比例） | 模板复用率、笔刷复用率 |

---

## 2. 目标用户与 JTBD

### 2.1 主要细分（按问题/场景，不按人群）

| 段位 | 用户画像 | 核心 Job-To-Be-Done | 现状替代方案 |
|------|---------|---------------------|------------|
| **S1 · 跟风创作者（最大池）** | 18–28 岁女生，小红书 / 抖音内容消费者，看到爆款想复刻 | 「我看到一张爆款封面，**最快**给自己的图也来一张」 | 醒图 + 截图复制爆款风格，效果差 80% |
| **S2 · 内容博主（高价值）** | 有几千到几万粉的图文/Vlog 博主 | 「我要稳定、有辨识度的视觉签名，且**过程感**比成品更带流量」 | Procreate + 屏幕录制 + 剪映拼接 |
| **S3 · 设计/字体爱好者（KOC 种子）** | 平面设计学生、字体邪修圈层 | 「我要一个**轻量造字工坊**，能调字形外框、内部疏密」 | PS 液化 + Glyphs / Illustrator |
| **S4 · 礼物 / 仪式场景（长尾）** | 给朋友/伴侣做生日卡、纪念封面 | 「在一张合照上写句话，感觉是亲手写的」 | 手写实拍 → 太丑或不够文艺 |

> **建议：MVP 阶段先服务 S1 + S2**，因为他们的「跟风 → 爆款 → 二次传播」闭环最短；S3 留作种子社区与模板供给方，S4 做营销话题。

### 2.2 核心 Job Story
> **当**我刷到一张「图片 + 手写字 Live 图」爆款时，
> **我想要**用我自己的照片，2 分钟内复刻出一张同样质感的 Live 图，
> **以便**发到小红书/朋友圈/IG，获得「这哪个 App 做的求教程」的评论。

---

## 3. 多视角 Ideation（Brainstorm）

> 按 PM / Designer / Engineer 三个视角各提 5–7 条，再聚合优先级。

### 3.1 PM 视角（业务价值 / 增长 / 差异化）

1. **「直接出 Live Photo」作为头号差异化卖点**（v0.2 升级）：所有传播物料、落地页第一句话都是「**做出来就是 Live 图，长按就能看过程**」，把这件事打磨成"竞品做不到的反差点"。
2. **「描爆款」一键模板包**：把当下小红书热门封面字体（如「邪修宋」「奶酥手写」「机能哥特」）打包成模板，每周更新，做成内容订阅型钩子。
3. **「水印即广告」分享机制**：导出 Live Photo 的 .MOV 第一帧右下角默认带可爱的角标 + 「用 XX 写出来」二维码，借鉴美图秀秀/醒图的传播飞轮。**注意**：水印必须只在视频部分加，封面 .HEIC 不加，避免破坏静态颜值。
4. **创作者激励 / 模板市场**：S3 用户产出的「字形 + 排版」组合可上传市场，被使用 1 次给积分，做成 UGC 模板源。
5. **付费墙位置（不要做错）**：免费给所有笔刷+导出图片+ MP4 视频，**真 Live Photo 一键导出 / 去水印 / 高级字体库**作为订阅项。
6. **裂变钩子**：「邀请好友帮我邪修这个字」→ 共编模式（短期可做轻量分享链接）。
7. **跟风 SEO**：每个爆款字体模板生成可索引的 H5 落地页，承接「XX 字体怎么做」搜索流量；落地页内嵌 Live Photo demo（H5 上可用 LivePhotosKit JS 预览）。
8. **节日/IP 营销**：七夕、生日、毕业季模板包；与字体厂商（汉仪/方正）联名授权字库子集。

### 3.2 Designer 视角（体验 / 易用性 / 愉悦感）

1. **三步线性主流程**（核心）：① 上图  ② 选字+套排版圈圈  ③ 描画。每一步都「可跳过返回」，但默认线性，避免新手在 Demo 1 多入口里迷路。
2. **「外框邪修」交互（图 1）**：标准字外面 4 个角 + 4 个边中点 = 8 个控制点，**任意拖成不规则四边形**（透视变换），同时显示半透明对照原字。
3. **「田字格中线轴」交互（图 2，v0.2 新方案，核心创新）**（取代之前的"液化"想法）：
   - 选中字后，字内部出现 **1 横轴 + 1 竖轴**（蓝色虚线），把字框成田字格 4 象限。
   - **拖竖轴**：左部首压缩、右部首扩张（适合左右结构字如「邪」「林」）。
   - **拖横轴**：上下部首挤压（适合上下结构字如「字」「思」）。
   - 实时显示比例提示「**左 35% / 右 65%**」浮动 hint。
   - **认知模型像小学生写字时的"米字格" / "田字格"**，0 教学成本。
   - 双击轴 = 重置为 50/50；与 8 控制点可叠加（先内部、再外框）。
4. **「魔法时刻」三重 WOW 设计**：
   - WOW 1：拖动田字格竖轴，字"被掐了腰"，瞬间获得邪修感（在选字阶段）。
   - WOW 2：点「隐藏参考」，所有辅助元素一秒消失，只剩自己的笔迹（在描画完成阶段）。
   - WOW 3：导出后回到相册，**长按图片，字开始一笔一划自己写出来**（这是最关键的 WOW，是用户从此不离开产品的钩子）。
5. **描画引导（来自 Demo 1）**：保留「圆圈编号」作为描画顺序提示，描完一笔编号自动消失，配「叮」的轻音效。
6. **草稿即作品**：每一次操作自动存草稿、可缩略图回看；导出时给三种产物（高清 PNG / MP4 / 真 Live Photo）。
7. **「之前/之后」切换**：长按图片可瞬间切回原图，让用户感受「魔法感」并截图对比，鼓励分享。
8. **新手 30 秒沉浸引导**：首次启动 → 强制走一遍预置「邪」字 demo，让用户第一次产物就是爆款级别。**重点教田字格中线轴**（最具魔法感、最易学），8 控制点放在进阶教程。
9. **Live Photo 引导文案设计**：「首次需要 5 秒装一次 Apple 自带的小工具，之后每次都是一键。」**绝对不能让用户感到"这是绕过限制的临时方案"**，要包装成"我们把 Apple 的能力直接接通了"。

### 3.3 Engineer 视角（技术可行性 / 复用 / 数据杠杆）

1. **复用 fabric.js 现有引擎**（Demo 1）：在同一画布上把「字模图层（参考字）」「排版圈圈图层」「笔刷图层」分层管理，避免重写。

2. **字形变形技术选型（v0.2 升级，更优解）**：
   - **F2.1 外框邪修（8 控制点 / 图 1）**：opentype.js 解析字 → SVG path → 4 角点求 **homography 矩阵** → SVG `matrix3d` 渲染。原生 GPU，30fps+。
   - **F2.2 内部邪修（田字格中线轴 / 图 2，核心创新）**：本质是 **2×2 mesh warp**：
     - 把字按 50%/50% 切成 4 象限（左上/右上/左下/右下）
     - 用 `<clipPath>` 截取每个象限 + `<g transform>` 缩放到目标矩形
     - 拖动竖轴 = 改变 vx（左半宽度比例），4 个象限的 transform 全部跟新
     - **状态只是 2 个浮点数 `{vx, hy}`，O(1) 操作，60fps 在 iPhone 8 都没压力**
     - **不需要液化、不需要 WebGL shader、不需要 WASM**，纯 CSS/SVG 就够
   - **为什么这个方案碾压"真液化"**：性能（GPU 合成层 vs O(N²) 像素操作）+ 可逆（2 个数 vs 整张位图）+ 矢量（导出无损 vs 栅格化）+ 认知（田字格 vs 液化笔刷）。
   - **真液化（Phase 3）**：只在 mesh warp 之外提供「精修笔刷」，覆盖剩余 10% 极端形变。

3. **录制改造（v0.2 升级）**：Demo 1 录制是按「path 顺序回放」的，要做到「先画字模骨架→再描笔刷」的两段式动画，需要把 drawHistory 拆成 `guide` 和 `ink` 两个轨道，导出时只保留 ink 轨道渲染但保留时序。**新增**：录制总时长固定 3 秒（Apple Live Photo 标准长度），按笔画数自动调速。

4. **真 Live Photo 端到端管线（v0.2 新增，已通过技术调研验证）**：

   ```
   fabric canvas 最后一帧 PNG
        │
        ├─→ libheif-js（WASM）→ .HEIC
        │       │
        │       └─→ @aidin36/xmp 写入 MakerNotes[17] = UUID
        │
   MediaRecorder 录制 webm
        │
        ├─→ FFmpeg.wasm 转 .MOV (H.264 + still-image-time track)
        │       │
        │       └─→ mp4box.js 写入 com.apple.quicktime.content.identifier = UUID
        │
   两文件打包 → Apple Shortcut deep link → 自动存为 Live Photo
   ```

   **关键库（全部开源、全部 WASM、0 后端）**：
   - `opentype.js` ~ 350KB
   - `libheif-js` ~ 2MB
   - `FFmpeg.wasm` ~ 11MB（gzip 3MB，懒加载）
   - `mp4box.js` ~ 100KB
   - `@aidin36/xmp` ~ 50KB

   **Apple Shortcut 路径**：用户首次安装一次「Save Live Photo」Shortcut（5 秒），之后每次浏览器 deep link 自动跳转执行，体验 ≈ 一键。

5. **字库供给**：MVP 用 1–3 套开源/可商用字体（如思源黑体、霞鹜文楷、得意黑），Phase 2 接入授权字库。

6. **离线优先 / 轻 PWA**：所有渲染本地完成（不传图），既快又规避隐私风险，是面向小红书/抖音用户的天然信任点。**特别注意**：Live Photo 编码也全部本地完成，**0 上传**，是相比所有竞品的隐私优势。

7. **AI 辅助点（克制使用）**：仅在「自动建议田字格切线位置（基于部首识别）」「自动建议笔刷+颜色（基于底图主色调）」做轻 AI，不做生成式造字以避免成本和版权坑。

8. **平台兜底**：
   - iOS 16+：默认走 Apple Shortcut 路径
   - iOS 14–15：降级 AirDrop 自己（系统会自动识别 .HEIC + .MOV 配对为 Live Photo）
   - Android：直接 MP4，**Phase 1.5 适配 Motion Photo（小米/OPPO/Google）**，用 XMP 嵌入视频
   - 微信内置浏览器：提示「请用系统浏览器打开」

---

## 4. Top 5 优先级 Idea（跨视角聚合，v0.2 重排）

> 按「与北极星目标对齐 × 影响 × 可行性 × 差异化」打分（5 分制，仅相对）。
> **v0.2 关键变化**：原 #2「8 控制点 + 真液化」拆分；**Live Photo 上升到 #1 优先级**。

| # | 想法 | 来自 | 对齐 | 影响 | 可行性 | 差异化 | 关键假设（待验证） |
|---|------|------|------|------|--------|--------|-------------------|
| **1** | **🆕 直接出真 iOS Live Photo（一键存相册）** | **P1+E4** | **5** | **5** | **4** | **5** | **用户愿意首次装 Shortcut；HEIC+MOV 配对在 iOS 16/17/18 全跑通** |
| 2 | **田字格中线轴 mesh warp（图 2 内部邪修）+ 8 控制点（图 1 外框邪修）** | D2/D3 + E2 | 5 | 5 | 5 | 5 | 触屏拖动田字格精度足够；mesh warp 在 iPhone XR 60fps |
| 3 | **三步线性主流程（上图→选字+排版→描画→导出 Live Photo）** | D1 | 5 | 5 | 5 | 4 | 用户能在 2 分钟内走完，且首次产物就达到「值得分享」 |
| 4 | **一键复刻爆款模板包（每周更新）** | P2 | 5 | 5 | 4 | 4 | 「跟风型」用户对模板新鲜度敏感，每周更新能拉日活 |
| 5 | **首次 30 秒沉浸式引导（重点教田字格中线轴）** | D8 | 4 | 4 | 5 | 3 | 引导后的次日留存 ≥ 不引导组的 1.5 倍 |

**砍掉/暂缓**：真液化（升 P3）、模板市场（P4）、共编（P6）、AI 生成造字（E7）、字库联名（P8）→ 全部 Phase 3 之后。

---

## 5. 关键风险假设（Risky Assumption Map，v0.2 增强）

> 沿用 PM Skills 的 Value / Usability / Viability / Feasibility 四象限。
> **v0.2 新增 4 条 Live Photo / 田字格相关的强假设。**

| 类型 | 假设 | 风险等级 | 验证手段（Phase 0–1） |
|------|------|---------|---------------------|
| **Value** | 用户愿意为「邪修字 Live Photo」付费/留存 | 🔴 高 | Fake Door：上线 Demo 1 加一个「真 Live 一键存」按钮，记录点击率与邮箱留资 |
| **Value** | 「Live Photo 长按看过程」比「MP4 视频」传播效率高 ≥ 50% | 🔴 高 | 种子用户 A/B：10 人发 MP4，10 人发 Live Photo，比较朋友圈/小红书互动率 |
| **🆕 Value** | **观众真的会主动「长按」朋友圈/小红书 feed 里的图** | 🟡 中 | 微信 Live 图行为数据（公开报告显示 Live 图长按率 12–20%）+ 自家测试 |
| **Usability** | 触屏 8 控制点能让 Mom-test 用户做出图 1 效果 | 🔴 高 | 5 人可用性测试，≤ 5 分钟做出邪修「邪」字 |
| **🆕 Usability** | **触屏拖动田字格中线轴能让 Mom-test 用户 30 秒内做出图 2 效果** | 🟡 中 | **5 人可用性测试，看是否能拖出"邪"字右半放大；这是田字格方案的核心可用性验证** |
| **🆕 Usability** | **首次安装 Apple Shortcut 不会被 ≥ 30% 用户放弃** | 🔴 高 | 上线初期监控 Shortcut 安装漏斗；如 > 30% 流失，加视频教程或换降级路径 |
| **Usability** | 排版圈圈 + 编号引导在小屏（iPhone SE）不会太挤 | 🟡 中 | 真机走查 320×568 屏幕 |
| **Viability** | 订阅墙（Live 一键导出 / 去水印 / 高级字体）能跑通 0.5%–1% 付费率 | 🟡 中 | Phase 1 上线就埋付费按钮，但暂不收款，看意愿点击 |
| **Feasibility** | F2.1 八控制点透视变换在 iPhone 12 ≥ 30fps | 🟢 低 | 技术 spike 1 天 |
| **🆕 Feasibility** | **F2.2 田字格 2×2 mesh warp 在 iPhone XR ≥ 60fps** | 🟢 低 | **2 小时 spike 即可，因为只是 4 个 SVG transform** |
| **🆕 Feasibility** | **HEIC+MOV 配对（带 metadata）在 iOS 16/17/18 Safari 都能跑通端到端** | 🟡 中 | **关键 spike：1.5 天，三机型实测，含 Shortcut 跳转回流** |
| **🆕 Feasibility** | **FFmpeg.wasm 11MB 懒加载在 4G 网络下 ≤ 5s** | 🟢 低 | 网络模拟测试，0.5 天 |
| **Feasibility** | 录制时区分「guide / ink」两轨 | 🟢 低 | 1 天改造 drawHistory |
| **Legal** | 字库版权（思源/霞鹜/得意黑都允许商用，但要在 About 页注明） | 🟢 低 | 法务/许可清单 |
| **🆕 Legal** | **使用 Apple Shortcuts 链路是否违反 App Store / Apple 服务条款** | 🟢 低 | 调研：livephoto.online 已用此方案 1+ 年，未被 Apple 干预；属合规使用 |

---

## 6. 建议的 MVP 切片（Phase 1，v0.2 升级）

> 「最小可惊艳」，目标是端到端跑通一条爆款生产线，所有点都对齐 Top 5 优先级。
> **v0.2 关键改动**：田字格中线轴 与 真 Live Photo 都升级为 P0。

```
[上传图片] → [选字模板] → [外框邪修：8 控制点(图 1)] → [内部邪修：田字格中线轴(图 2)]
                                                                   ↓
[排版圈圈套用] → [选笔刷 + 描画] → [一键隐藏参考] → [导出真 iOS Live Photo]
                                                            ↓
                                               iOS：跳 Apple Shortcut 一键存相册
                                               Android：MP4 + 后续 Motion Photo 适配
```

**MVP 必须要有（P0）**：
- 1 套字库（默认霞鹜文楷，文艺调性匹配 S1 跟风用户）
- ✅ **F2.1 八控制点外框透视（图 1 能力）**
- ✅ **F2.2 田字格中线轴 2×2 mesh warp（图 2 能力，核心创新）**
- 4–6 个排版模板（来自 Demo 2）
- Demo 1 的 6 种笔刷全保留
- ✅ **真 iOS Live Photo 输出 + Apple Shortcut 一键路径（核心创新）**
- MP4 兜底（Android & iOS 拒装 Shortcut 用户）
- 默认带角标水印 + UTM 二维码

**MVP 不做（推迟）**：
- 真液化笔刷（图 2 完整版 → Phase 3，田字格已覆盖 90%）
- 3×3 井字格（Phase 2）
- 模板市场 / UGC
- AI 建议
- iOS 原生 App 壳（Phase 2 立项，承接 P0 Shortcut 路径的体验升级）
- Android Motion Photo（Phase 1.5）

---

## 7. 待回答的问题（建议下一步先问 / 先验证，v0.2 增量到 9 个）

1. **平台优先级**：MVP 先做 H5（基于现有 Demo 调）、还是直接做 iOS App？  → **建议 H5 先上 + Phase 2 立 iOS App 壳，因为 H5 的 Apple Shortcut 路径已经验证可行，能 6 周出 MVP；iOS App 壳是体验升级路径**。
2. **目标用户优先级**：S1（跟风用户）vs S3（设计师种子）哪个先做？（决定字库丰富度与教程文案语调）
3. **盈利模式**：免费导出图 + 付费 Live Photo 一键存？还是 7 天试用全功能？还是模板订阅？
4. **品牌/调性**：要走「醒图」式大众傻瓜路线，还是「Procreate」式专业潮酷路线？（决定 UI 风格、命名）
5. **数据后端**：MVP 是否需要登录/账号体系？（无登录可加快上线，但失去复访留存数据）
6. **🆕 Apple Shortcut 自家做 vs 借用**：自家做体验更可控、可埋点、有 brand；借用（如 livephoto.online 现成版）上线快但不可控。**建议自家做**，因为这是核心体验链路。
7. **🆕 田字格中线轴默认显示 vs 手动开启**：默认显示 → 教育成本 0 但首屏视觉重；手动开启 → 干净但用户可能找不到这个核心功能。**建议默认显示，但只在选中字时才出现**。
8. **🆕 F2.1 外框 vs F2.2 田字格，谁先教**：建议**首次引导只教田字格**（更直观、更有魔法感），8 控制点放进阶教学，避免新手被两套交互同时压垮。
9. **🆕 Live Photo 失败时的降级策略**：跳 Shortcut 失败 → 降级到 AirDrop 自己 → 还失败 → 给 MP4。需要把这个降级链路 UI 流程画出来。

---

## 8. 下一步建议（v0.2 增量）

1. ✅ 用本探索文档对齐方向，挑选 1 个工作名。
2. ⏭️ 阅读 `PRD-手写Live.md`，对照 P0/P1/P2 切片确认 MVP 范围（**重点看 §6.3 F2 和 F5，是 v0.2 改动最大的两节**）。
3. ⏭️ 跑一次 **Pre-mortem**（`/pre-mortem`）找隐藏风险，**重点针对 Live Photo 链路 + 田字格交互**。
4. ⏭️ 用 5 个真实用户做可用性测试（邀请刷过手写字爆款的人优先），重点验证：
   - 🆕 田字格中线轴的可用性（A5 假设）
   - 🆕 Apple Shortcut 安装意愿（A6 假设）
   - 8 控制点的可用性（A4 假设）
5. ⏭️ Phase 0 三个核心技术 spike（**总计 2.5 天，建议尽早做**）：
   - 🆕 田字格 2×2 mesh warp 性能（2 小时）
   - 🆕 HEIC + MOV 配对 + Shortcut 跳转端到端（1.5 天）
   - 🆕 FFmpeg.wasm 懒加载性能（0.5 天）
6. ⏭️ 跑一遍 livephoto.online，亲身体验完整 Live Photo 路径，了解用户体验细节（30 分钟）。

---

## 附录 · v0.1 → v0.2 关键变更速览

| 维度 | v0.1（旧） | v0.2（新） |
|------|-----------|-----------|
| Live Photo | Phase 2，先用 9:16 MP4 替代 | **P0，MVP 必须出真 Live Photo** |
| 内部邪修方案 | "拆部首 + 真液化"（P1+P2） | **田字格中线轴 2×2 mesh warp（P0）** |
| 字形变形性能预算 | 30fps | 60fps（mesh warp）+ 30fps（透视） |
| Top 5 优先级 #1 | 三步线性主流程 | **直接出真 Live Photo** |
| Phase 0 时长 | 1 周 | 1–2 周（增加 2 个核心 spike） |
| MVP 上线时长 | 6 周 | 6 周（不变，仅 spike 多 0.5–1 周） |
| 核心新增技术栈 | — | libheif-js / FFmpeg.wasm / mp4box.js / Apple Shortcuts |

